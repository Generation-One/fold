[target.x86_64-pc-windows-msvc]
linker = "C:\\Program Files\\LLVM\\bin\\lld-link.exe"
rustflags = [
    # Parallel linking with lld
    "-C", "link-arg=-threads:8",
]

[build]
# Use all available CPU cores for parallel compilation
jobs = 8
