# =============================================================================
# Fold Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8765
PUBLIC_URL=http://localhost:8765

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_PATH=./data/fold.db
QDRANT_URL=http://localhost:6334

# -----------------------------------------------------------------------------
# Storage
# -----------------------------------------------------------------------------
ATTACHMENTS_PATH=./data/attachments
SUMMARIES_PATH=./data/summaries
MAX_ATTACHMENT_SIZE=10485760  # 10MB

# -----------------------------------------------------------------------------
# Embeddings
# -----------------------------------------------------------------------------
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# -----------------------------------------------------------------------------
# Session
# -----------------------------------------------------------------------------
SESSION_SECRET=change-this-to-random-32-bytes-string
SESSION_MAX_AGE=604800  # 7 days in seconds

# -----------------------------------------------------------------------------
# Admin Bootstrap (remove after first admin created)
# -----------------------------------------------------------------------------
ADMIN_BOOTSTRAP_TOKEN=your-secret-bootstrap-token

# =============================================================================
# Auth Providers
# =============================================================================
# Pattern: AUTH_PROVIDER_{NAME}_{FIELD}
# NAME is arbitrary (uppercase) - used in URLs like /auth/login/{name}
#
# Required fields: TYPE, CLIENT_ID, CLIENT_SECRET
# Optional fields: DISPLAY_NAME, ISSUER, SCOPES, ICON, ENABLED

# -----------------------------------------------------------------------------
# Example: Corporate OIDC (Zitadel, Keycloak, Okta, etc.)
# -----------------------------------------------------------------------------
# AUTH_PROVIDER_CORPORATE_TYPE=oidc
# AUTH_PROVIDER_CORPORATE_DISPLAY_NAME=Corporate SSO
# AUTH_PROVIDER_CORPORATE_ISSUER=https://auth.company.com
# AUTH_PROVIDER_CORPORATE_CLIENT_ID=your-client-id
# AUTH_PROVIDER_CORPORATE_CLIENT_SECRET=your-client-secret
# AUTH_PROVIDER_CORPORATE_SCOPES=openid profile email
# AUTH_PROVIDER_CORPORATE_ICON=key

# -----------------------------------------------------------------------------
# Example: Google
# -----------------------------------------------------------------------------
# AUTH_PROVIDER_GOOGLE_TYPE=oidc
# AUTH_PROVIDER_GOOGLE_DISPLAY_NAME=Google
# AUTH_PROVIDER_GOOGLE_ISSUER=https://accounts.google.com
# AUTH_PROVIDER_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# AUTH_PROVIDER_GOOGLE_CLIENT_SECRET=xxx
# AUTH_PROVIDER_GOOGLE_ICON=google

# -----------------------------------------------------------------------------
# Example: GitHub (OAuth2, not OIDC)
# -----------------------------------------------------------------------------
# AUTH_PROVIDER_GITHUB_TYPE=github
# AUTH_PROVIDER_GITHUB_DISPLAY_NAME=GitHub
# AUTH_PROVIDER_GITHUB_CLIENT_ID=xxx
# AUTH_PROVIDER_GITHUB_CLIENT_SECRET=xxx
# AUTH_PROVIDER_GITHUB_ICON=github

# -----------------------------------------------------------------------------
# Example: GitLab (self-hosted)
# -----------------------------------------------------------------------------
# AUTH_PROVIDER_GITLAB_TYPE=oidc
# AUTH_PROVIDER_GITLAB_DISPLAY_NAME=GitLab
# AUTH_PROVIDER_GITLAB_ISSUER=https://gitlab.company.com
# AUTH_PROVIDER_GITLAB_CLIENT_ID=xxx
# AUTH_PROVIDER_GITLAB_CLIENT_SECRET=xxx
# AUTH_PROVIDER_GITLAB_ICON=gitlab

# =============================================================================
# LLM Providers (in fallback order)
# =============================================================================
# Configure at least one. Fold tries them in priority order:
# 1. Gemini (free tier)
# 2. OpenRouter (has free models)
# 3. OpenAI

# Gemini (priority 1 - free)
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# OpenRouter (priority 2)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3-8b-instruct:free

# OpenAI (priority 3)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Git Integration (optional)
# =============================================================================
# GitHub App for better rate limits (optional)
# GITHUB_APP_ID=
# GITHUB_APP_PRIVATE_KEY=
